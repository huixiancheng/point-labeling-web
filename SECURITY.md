# Security policy

## 默认边界

默认 Windows 服务只绑定 `127.0.0.1`，用于本机浏览器访问。不要把服务端口直接暴露到局域网或公网；如果确实需要远程访问，应自行增加访问控制、反向代理和防火墙规则。

## 报告问题

请不要在公开 Issue 中发布密码、令牌、私有数据或可利用漏洞的完整细节。仓库启用 GitHub Security Advisories 后，优先通过私有安全报告提交；在未启用之前，请先提交不包含敏感内容的 Issue，说明希望使用私下渠道继续沟通。

依赖安全问题也请附上受影响的 lockfile、版本和最小复现方式。发布前使用 `npm audit --audit-level=high` 检查前端开发依赖。
